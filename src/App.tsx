import { Layout } from 'antd';

const { Header, Content, Footer } = Layout;

function App() {
	return (
		<Layout>
			<Header></Header>
			<Content></Content>
			<Footer></Footer>
		</Layout>
	);
}

export default App;
